# 0. 核心接力策略（最高执行优先级）

Step08 dwd-h: drill decision/failure modes; do not redo Step05; dags/ only; next dwd-h: forbid daily create_external_sensor waiting qpon_ods_h/qpon_dim_h - use Skip like dim_h; hour order JOIN ON id not cross-shard unique; verify upstream tasks still live; if reading daily pr

**[执行准则]**: 以上为上一步指挥官转交的"强制任务"。你必须优先响应并回显证据，否则将被判定为考古失败。

# 0.5 项目军规（项目级行为约束）

意图路由：先读知识库入口再改代码；Skill 优先调度流水线。
强制红线：禁止 macOS 专有 sed；路径锚定；Shell set -euo pipefail。
双写要求：改调度逻辑须同步更新知识库。
本轮相关约束：扫描权威范围为 dags/；产出写入 qpon-bigdata-knowledge/；NON_JAVA 语义映射。

**[执行准则]**: 项目军规对本步分析与写回具有高优先级约束，不得被普通先验信息覆盖。

【项目类型说明】本项目经结构探测确认为非 Java/Maven 项目（ACTUAL_MODULE_PREFIX=NON_JAVA）。
模板中涉及 Java 特有路径（如 pom.xml、-start/、-app/、-dao/、-client/ 等）和 Java 特有概念
（如 DubboReferenceConfig、MyBatis、ShardingSphere、RocketMQ 等）的扫描节，执行 Agent 应：
1. 识别该节的分析意图（如「提取外部依赖契约」、「还原数据模型」、「审计异步机制」等）；
2. 将意图映射到本项目实际存在的等价物（如 Airflow DAG、Python 算子、SQL/BigQuery、配置文件、CLI 工具链等）；
3. 对无等价物的扫描节明确标注「N/A：本项目无对应结构，原因：[一句话说明]」，不跳过、不虚构。
4. **工作区边界约束覆盖**：模板中所有「若 `<project>-xxx/` 不存在 → 必须立即停止」的硬熔断指令，在 NON_JAVA 模式下**自动失效**，改为按上述第 1-3 条规则处理。禁止因 Java 路径不存在而触发熔断。
模板是引导分析思考的框架，不是死脚本，请充分发挥语义推断能力产出等深度的知识库内容。

【扫描边界硬约束】本次知识库的代码事实权威范围严格限定为工作区内的 `dags/` 目录（含子目录）。
禁止以仓库其他目录（如 scripts/、ai-knowledge-knowledge/）作为业务事实来源；仅可读取协作协议与产出目录。

# Step 08: 业务模块深潜模板

[Role] 业务语义深度解读专家。
你的任务是为 DWD明细小时批 模块生成业务语义深度解读文档——
不重复 Step 05 的调用链追踪，而是向下钻取业务决策点、扩展机制、失败模式。

[Context]
我们正在为 qpon-bigdata（qpon-bigdata）构建 AI 可加载的项目知识库。

核心前提：Step 05（05_Business_Orchestration.md）已经完成了：
- RPC 实现类的方法入口映射
- 核心链路的调用链追踪
- 非主线功能清单
- DDD 分层审计和设计模式识别

本步骤不重复 Step 05 的工作，而是以 05 为骨架，向下钻取业务语义。

[最高指令挂载]
在执行任何动作前，必须强制静默读取并绝对服从本项目的底层协作法典
（位于 .cursor/rules/collaboration-protocol.mdc 或 .gemini/rules/collaboration-protocol.md 根据环境加载），
你接下来的所有响应步调与输出规范，必须以该协议为最高准则。

[先验知识注入]
请静默读取以下文件，建立先验认知：
1. qpon-bigdata-knowledge/05_Business_Orchestration.md — 核心链路和调用链
2. qpon-bigdata-knowledge/04_Data_Model_and_Lifecycle.md — 数据模型
3. qpon-bigdata-knowledge/03_Downstream_Dependencies.md — 下游依赖


[Task: DWD明细小时批 模块深度解读]

### 扫描范围

**核心类清单**（从 Step 05 提取）：
- - `qpon_dwd_h/qpon_dwd_h.py`
- `qpon_dwd_h`
- `qpon_ods_h`
- `qpon_dim_h`
- `qpon_dws_h/qpon_dws_h.py`
- `qpon_dws_h`
- `qpon_rpt_h/qpon_rpt_h.py`
- `qpon_rpt_h`

**扫描目标**：
- 逐类读取至 EOF，理解业务逻辑
- 重点关注：决策点、条件分支、异常处理、扩展点

### 提取任务

#### A. 模块定位（固定骨架）
一句话定义该模块在整个服务中的职责边界。

#### B. 核心类清单（固定骨架）
列出该模块的核心类（从 Step 05 提取），标注每个类的职责。

格式：
| 类名 | 类型（Service/Executor/Handler/Checker/Repository） | 职责 |

#### C. 入口方法（固定骨架）
列出该模块对外暴露的入口方法（RPC 接口方法或被其他模块调用的公共方法）。

格式：
| 入口方法 | 调用方 | 一句话描述 |

#### D. 调用链（固定骨架）
引用 Step 05 中该模块的核心链路调用链，不重复追踪。

#### E. 前序步骤验证（固定骨架）
验证 Step 01-07 中与该模块相关的发现：
- Step 02：该模块涉及的对外契约
- Step 03：该模块调用的下游依赖
- Step 04：该模块操作的数据实体
- Step 06：该模块涉及的异步机制
- Step 07：该模块涉及的配置和缓存

#### F. 衍生约束清单（固定骨架）
基于该模块的代码事实，提炼出 AI 开发时必须遵守的约束。

**输出规则**：
- 每条约束不超过 3 行（约束内容 + 代码证据 + 违反后果）
- 至少 3 条，不超过 10 条；若决策点少于 5 个则不强求超过 5 条
- 只提炼强执行约束（违反有明确后果），不写「建议」类软约束
- **跨模块技术债必须列出**：若本模块存在「分片表查询无分片键」「app 层直接操作 Repository」等在 04/05 已标注的🔴违规，必须在 §F 中以独立约束条目出现，标注「已知技术债，新增同类操作禁止复制此模式」，不允许仅在业务特性章节中作为风险描述一带而过
- **幂等性约束必须独立列出**：若模块有幂等校验机制，必须单独提炼为一条约束，明确幂等键是什么

格式：
| 约束 ID | 约束内容（一句话，可执行） | 代码证据（类名.方法名） | 违反后果 |

#### G-J. 业务特性章节（开放特性）
基于该模块的代码特点，自行设计 2~4 个特性章节（标记为 G/H/I/J）。

**设计原则**：
- 如果模块有复杂的校验链 → 添加"校验链详解"章节
- 如果模块有模板方法模式 → 添加"模板方法详解"章节
- 如果模块有补偿机制 → 添加"补偿策略详解"章节
- 如果模块有策略模式 → 添加"策略模式详解"章节
- 如果模块有状态机 → 添加"状态流转详解"章节
- 如果模块有缓存策略 → 添加"缓存策略详解"章节
- 如果模块有分布式锁 → 添加"并发控制详解"章节

**每个特性章节的输出规则**：
- 不超过 60 行
- 包含：业务背景（1~3句）、实现方式（关键类名+流程）、关键决策点（逐条列出）、失败模式（排查方法）
- 决策点用「类名.方法名 — 判断条件 → 分支结果」格式，不粘贴代码

[Action]
在 qpon-bigdata-knowledge/ 目录下生成 08f_Module_DWD明细小时批.md

[Constraint - 工业级底线]

**工作区边界**：所有文件访问必须限定在当前工作区根目录下。若核心类清单中的类在工作区内不存在，必须立即停止并输出：`❌ 未在工作区找到 [类路径]，无法完成深潜。请确认 PROJECT_NAME 与目标项目一致。` 禁止推断替代路径，禁止访问工作区外目录。

**行数上限**：全文不超过 400 行。超出时按以下优先级裁剪：
固定骨架（A~F）完整 > G~J 每章节 ≤ 60 行 > 业务特性章节数量 ≥ 2

**输出格式锁定**：
- 固定骨架（A~F）必须包含，所有模块统一
- 业务特性章节（G~J）自行决定，不同模块可以不同，数量 2~4 个

**严防静默截断**：
- 核心类清单必须完整，不允许遗漏
- 衍生约束清单至少 3 条，每条不超过 3 行
- 业务特性章节至少 2 个，每个不超过 60 行

**专业性底线**：
- 决策点用代码位置标注（类名.方法名），不写行号
- 失败模式基于代码事实，不臆测
- 约束内容具体可执行，不用模糊词汇
- 不粘贴原始代码片段，用结构化描述替代

**结尾标准审计闭环**：

```
> [!SUCCESS] DWD明细小时批 模块深潜闭环验证
> - 扫描范围：[N] 个核心类
> - 提取结果：[X] 个入口方法、[Y] 条衍生约束、[Z] 个业务特性章节
> - 全文行数：[N] 行（≤ 500 行）
> - 前序验证：Step 02 [结论] / Step 03 [结论] / Step 04 [结论]
> - EOF 状态：已确认遍历至最后一行，无静默截断

> [!RELAY] 定向审计约束
> - **物理事实 (Context)**: [本模块深潜发现的、对下一个模块或收官阶段有决定性影响的关键事实]
> - **推演约束 (Constraint)**: [基于本模块发现，强制下一步重点关注的跨模块依赖或风险点]
> - **物理锚点 (Anchors)**: [对应核心类文件路径及关键方法行号]
```

**重要额外指令：完成所有分析和文件写入后，必须在响应的最后原样输出 [!SUCCESS] 审计闭环块到控制台 Stdout，以便指挥官提取。禁止仅写入文件。**

**[!SUCCESS] 写入回执（固定字段，必须输出）**
- WRITE_TARGET: <本步目标知识文件相对路径>
- WRITE_RESULT: UPDATED | NO_CHANGE
- WRITE_BYTES: <写入后文件字节数，整数>
- WRITE_SHA256: <写入后文件 SHA256>
- NO_CHANGE_REASON: <仅当 WRITE_RESULT=NO_CHANGE 时必填；否则写 N/A>

约束：
1) WRITE_RESULT=UPDATED 时，WRITE_BYTES 与 WRITE_SHA256 必须基于写入后的真实文件；
2) WRITE_RESULT=NO_CHANGE 时，必须给出 NO_CHANGE_REASON，禁止留空；
3) 若无法确认以上字段的真实性，必须显式宣告失败，禁止输出伪造回执。
