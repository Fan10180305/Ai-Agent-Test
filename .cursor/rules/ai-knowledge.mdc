---
description: ai-knowledge 核心意图路由与防御性编码军规
globs: **/*.md, **/*.sh, **/*.py
alwaysApply: false
---

# ai-knowledge 开发军规 (Project Rules)

> 本规则适用于 `ai-knowledge` 项目（自动化考古流水线）的维护与演进。

## 1. 意图路由与知识资产
- **全局入口**：在进行任何代码修改前，必须先阅读 `ai-knowledge-knowledge/00_Master_Catalog.md`。
- **Skill 优先**：考古流水线的调度操作应优先通过 `archaeology-commander` Skill 进行，严禁直接绕过脚本逻辑修改产出。
- **双写约束**：若修改了 `run-archaeology.sh` 中的变量名、路径或步骤逻辑，必须同步更新 `ai-knowledge-knowledge/` 下对应的 01-08 知识库文件。

## 2. 防御性编码红线 (Hard Red Flags)
- **严禁 macOS 专有 Sed**：禁止在脚本中使用 `sed -i ''`。应使用兼容 POSIX 的 `sed -i` 或提供平台判断逻辑，防止在 Linux/CI 环境崩溃。
- **路径锚定**：所有文件操作必须基于项目根目录或 `config.json` 中的变量（如 `${TOOL_HOME}`, `${OUTPUT_DIR}`），严禁硬编码绝对路径（如 `/Applications` 或 `/Users`）。
- **Shell 鲁棒性**：所有 `.sh` 文件必须在首行声明 `set -euo pipefail`。严禁使用 `2>/dev/null` 掩盖可能导致流水线逻辑中断的错误。
- **Python 监控健壮性**：修改心跳监控逻辑时，需确保 Python 脚本块在 JSONL 格式损坏或 `python3` 缺失时能通过 `try...except` 或 `|| true` 降级，不得阻塞主进程。
- **Token 预算管理**：在指挥官（001）生成 Prompt 的逻辑中，严禁一次性注入超过 20 个大文件，应通过分步读取或摘要提取（`[!SUCCESS]` 块）进行上下文裁剪。

## 3. 协作与交付协议
- **审计闭环**：所有新增加的考古步骤产出，必须在文档前 25 行内包含 `[!SUCCESS]` 审计闭环块。
- **断点保护**：在实现“断点续跑”功能前，所有对产出目录的操作必须具备幂等性（覆盖写而非追加写）。
- **进程生命周期**：所有孵化的后台进程必须注册到 `trap '...' EXIT` 中，严禁产生僵尸 Python 进程。

## 4. 关键路径与物理锚点
- **核心调度器**：`scripts/knowledge-archaeology/run-archaeology.sh`
- **安装器**：`install.sh`
- **认知劫持防御**：`run-archaeology.sh:L205` (`_ensure_protocol` 强制注入)
- **Sed 冲突证据**：`run-archaeology.sh:L265` (macOS 专有 `sed -i ''`)
- **心跳监控锚点**：`run-archaeology.sh:L457` (Python3 依赖)
- **规则协议源**：`.gemini/rules/collaboration-protocol.md`

