# Step 08: 业务模块深潜模板

[Role] 业务语义深度解读专家。
你的任务是为 archaeology 模块生成业务语义深度解读文档——
不重复 Step 05 的调用链追踪，而是向下钻取业务决策点、扩展机制、失败模式。

【项目类型说明】本项目经结构探测确认为非 Java/Maven 项目（ACTUAL_MODULE_PREFIX=NON_JAVA）。
模板中涉及 Java 特有路径（如 pom.xml、-start/、-app/、-dao/、-client/ 等）和 Java 特有概念
（如 DubboReferenceConfig、MyBatis、ShardingSphere、RocketMQ 等）的扫描节，执行 Agent 应：
1. 识别该节的分析意图（如「提取外部依赖契约」、「还原数据模型」、「审计异步机制」等）；
2. 将意图映射到本项目实际存在的等价物（如 Shell/Python 脚本、配置文件、JSON 契约、CLI 工具链等）；
3. 对无等价物的扫描节明确标注「N/A：本项目无对应结构，原因：[一句话说明]」，不跳过、不虚构。
4. **工作区边界约束覆盖**：模板中所有「若 ai-knowledge-xxx/ 不存在 → 必须立即停止」的硬熔断指令，在 NON_JAVA 模式下**自动失效**，改为按上述第 1-3 条规则处理。禁止因 Java 路径不存在而触发熔断。
模板是引导分析思考的框架，不是死脚本，请充分发挥语义推断能力产出等深度的知识库内容。

[Context]
我们正在为 ai-knowledge（ai-knowledge）构建 AI 可加载的项目知识库。

核心前提：Step 05（05_Business_Orchestration.md）已经完成了：
- RPC 实现类的方法入口映射
- 核心链路的调用链追踪
- 非主线功能清单
- DDD 分层审计和设计模式识别

本步骤不重复 Step 05 的工作，而是以 05 为骨架，向下钻取业务语义。

[最高指令挂载]
在执行任何动作前，必须强制静默读取并绝对服从本项目的底层协作法典
（位于 .cursor/rules/collaboration-protocol.mdc 或 .gemini/rules/collaboration-protocol.md 根据环境加载），
你接下来的所有响应步调与输出规范，必须以该协议为最高准则。

# 0. 核心接力策略（最高执行优先级）

- **物理事实 (Context)**: `commander` 模块通过 `SKILL.md` 的状态机实现了高度复杂的变量替换与子进程管理，特别是 `ACTUAL_MODULE_PREFIX` 的探测逻辑直接决定了后续所有步骤的路径扫描行为。
- **推演约束 (Constraint)**: 下一步（考古执行模块 archaeology）需重点关注 `SKILL.md` 中定义的 `[!SUCCESS]` 提取规约在各 Prompt 模板中的物理实现，确保各模块深潜能正确输出标准摘要。
- **物理锚点 (Anchors)**: `SKILL.md` L236 (变量替换), L458 (子进程孵化), `run-archaeology.sh` L35 (路径推导)。

**[执行准则]**: 以上为上一步指挥官转交的"强制任务"。你必须优先响应并回显证据，否则将被判定为考古失败。

# 0.5 项目军规（项目级行为约束）

意图路由：修改前必须强制读取 `ai-knowledge-knowledge/00_Master_Catalog.md`。
强制红线：严禁静默失败；禁止 macOS 风格 `sed -i ''`；路径必须基于 config.json 或根目录；严禁泄露密钥。
双写要求：代码变更必须同步更新 00-08 知识库文件。
本轮相关约束：当前为模块深潜（Archaeology），需审计执行子进程的生命周期、结果审计规约及接力链条的完整性。

**[执行准则]**: 项目军规对本步分析与写回具有高优先级约束，不得被普通先验信息覆盖。

[先验知识注入]
请静默读取以下文件，建立先验认知：
1. ai-knowledge-knowledge/05_Business_Orchestration.md — 核心链路和调用链
2. ai-knowledge-knowledge/04_Data_Model_and_Lifecycle.md — 数据模型
3. ai-knowledge-knowledge/03_Downstream_Dependencies.md — 下游依赖
---
## 演进模式

本次为再次运行，存在上一轮产出的旧知识库。

请 read_file 读取 `ai-knowledge-knowledge/08b_Module_archaeology.md` ，将其作为「旧假说」参照：
- 代码事实是唯一权威；旧假说仅作参照，不得凌驾于代码之上
- 旧假说与代码不符时，以代码事实修正对应内容
- 代码中存在但旧假说未记录的逻辑，补充进对应章节
- 若代码事实未变化：优先保持旧文档高价值结构与表达，禁止仅因风格变化进行大面积重写
- 若删除旧章节/旧表格：必须给出代码证据锚点，否则视为退化性写回

### 演进对比输出要求（可审计格式）

在 `[!SUCCESS]` 前 20 行内输出以下字段：
- `事实修正：[xx]`
- `章节保持：[xx]`
- `章节补充：[xx]`
- `章节重写：[xx]`
- `删除章节：[xx]`
- `结构调整原因：[一句话，如无则写 无]`
- `无意义重写判定：[是/否]`
- `最小证据：[若无意义重写判定=是，至少 1 条：被重写章节 + 变化类型 + 代码事实未变化说明；否则写 无]`
- `退化风险申报：[触发时填写 受影响资产 + 变化类型 + 代码证据锚点 + 是否接受本次写回；未触发写 无]`

在 `[!RELAY]` 的 Context 字段中，若演进发现对下一步有决定性影响的变化（如：旧假说中某服务已删除/新增关键调用），必须声明。无演进变化时按常规填写。
---

[Task: archaeology 模块深度解读]

### 扫描范围

**核心类清单**（从 Step 05 提取）：
- `run_step()`: 考古原子步骤执行器 (run-archaeology.sh)
- `_extract_success()`: 审计块提取算子 (run-archaeology.sh)
- `_extract_relay()`: 接力策略提取算子 (run-archaeology.sh)
- `_extract_transcript_hint()`: 进度摘要提取算子 (run-archaeology.sh)
- `prompts/step-*.md`: 考古指令集模板库

**扫描目标**：
- 逐类读取至 EOF，理解业务逻辑
- 重点关注：决策点、条件分支、异常处理、扩展点

### 提取任务

#### A. 模块定位（固定骨架）
一句话定义该模块在整个服务中的职责边界。

#### B. 核心类清单（固定骨架）
列出该模块的核心类（从 Step 05 提取），标注每个类的职责。

格式：
| 类名 | 类型（Service/Executor/Handler/Checker/Repository） | 职责 |

#### C. 入口方法（固定骨架）
列出该模块对外暴露的入口方法（RPC 接口方法或被其他模块调用的公共方法）。

格式：
| 入口方法 | 调用方 | 一句话描述 |

#### D. 调用链（固定骨架）
引用 Step 05 中该模块的核心链路调用链，不重复追踪。

#### E. 前序步骤验证（固定骨架）
验证 Step 01-07 中与该模块相关的发现：
- Step 02：该模块涉及的对外契约
- Step 03：该模块调用的下游依赖
- Step 04：该模块操作的数据实体
- Step 06：该模块涉及的异步机制
- Step 07：该模块涉及的配置和缓存

#### F. 衍生约束清单（固定骨架）
基于该模块的代码事实，提炼出 AI 开发时必须遵守的约束。

**输出规则**：
- 每条约束不超过 3 行（约束内容 + 代码证据 + 违反后果）
- 至少 3 条，不超过 10 条；若决策点少于 5 个则不强求超过 5 条
- 只提炼强执行约束（违反有明确后果），不写「建议」类软约束
- **跨模块技术债必须列出**：若本模块存在「分片表查询无分片键」「app 层直接操作 Repository」等在 04/05 已标注的🔴违规，必须在 §F 中以独立约束条目出现，标注「已知技术债，新增同类操作禁止复制此 模式」，不允许仅在业务特性章节中作为风险描述一带而过
- **幂等性约束必须独立列出**：若模块有幂等校验机制，必须单独提炼为一条约束，明确幂等键是什么

格式：
| 约束 ID | 约束内容（一句话，可执行） | 代码证据（类名.方法名） | 违反后果 |

#### G-J. 业务特性章节（开放特性）
基于该模块的代码特点，自行设计 2~4 个特性章节（标记为 G/H/I/J）。

**设计原则**：
- 如果模块有复杂的校验链 → 添加"校验链详解"章节
- 如果模块有模板方法模式 → 添加"模板方法详解"章节
- 如果模块有补偿机制 → 添加"补偿策略详解"章节
- 如果模块有策略模式 → 添加"策略模式详解"章节
- 如果模块有状态机 → 添加"状态流转详解"章节
- 如果模块有缓存策略 → 添加"缓存策略详解"章节
- 如果模块有分布式锁 → 添加"并发控制详解"章节

**每个特性章节的输出规则**：
- 不超过 60 行
- 包含：业务背景（1~3句）、实现方式（关键类名+流程）、关键决策点（逐条列出）、失败模式（排查方 法）
- 决策点用「类名.方法名 — 判断条件 → 分支结果」格式，不粘贴代码

[Action]
在 ai-knowledge-knowledge/ 目录下生成 08b_Module_archaeology.md

[Constraint - 工业级底线]

**工作区边界**：所有文件访问必须限定在当前工作区根目录下。若核心类清单中的类在工作区内不存在， 必须立即停止并输出：`❌ 未在工作区找到 [类路径]，无法完成深潜。请确认 PROJECT_NAME 与目标项目一 致。` 禁止推断替代路径，禁止访问工作区外目录。

**行数上限**：全文不超过 400 行。超出时按以下优先级裁剪：
固定骨架（A~F）完整 > G~J 每章节 ≤ 60 行 > 业务特性章节数量 ≥ 2

**输出格式锁定**：
- 固定骨架（A~F）必须包含，所有模块统一
- 业务特性章节（G~J）自行决定，不同模块可以不同，数量 2~4 个

**严防静默截断**：
- 核心类清单必须完整，不允许遗漏
- 衍生约束清单至少 3 条，每条不超过 3 行
- 业务特性章节至少 2 个，每个不超过 60 行

**专业性底线**：
- 决策点用代码位置标注（类名.方法名），不写行号
- 失败模式基于代码事实，不臆测
- 约束内容具体可执行，不用模糊词汇
- 不粘贴原始代码片段，用结构化描述替代

**结尾标准审计闭环**：

```
> [!SUCCESS] archaeology 模块深潜闭环验证
> - 扫描范围：[N] 个核心类
> - 提取结果：[X] 个入口方法、[Y] 条衍生约束、[Z] 个业务特性章节
> - 全文行数：[N] 行（≤ 500 行）
> - 前序验证：Step 02 [结论] / Step 03 [结论] / Step 04 [结论]
> - EOF 状态：已确认遍历至最后一行，无静默截断

> [!RELAY] 定向审计约束
> - **物理事实 (Context)**: [本模块深潜发现的、对下一个模块或收官阶段有决定性影响的关键事实]
> - **推演约束 (Constraint)**: [基于本模块发现，强制下一步重点关注的跨模块依赖或风险点]
> - **物理锚点 (Anchors)**: [对应核心类文件路径及关键方法行号]
```

**重要额外指令：完成所有分析 and 文件写入后，必须在响应的最后原样输出 [!SUCCESS] 审计闭环块到控制台 Stdout，以便指挥官提取。禁止仅写入文件。**
